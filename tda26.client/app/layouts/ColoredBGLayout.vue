<script setup lang="ts">
    import { useSlots } from "vue";
    import ColoredBackground from "@/components/backgrounds/ColoredBackground.vue";

    const props = defineProps({
        text: {
            type: String,
            default: `zadej text jako text="nadpis"`
        }
    });

    const slots = useSlots();
</script>

<template>
    <div :class="$style.sections">
        <section :class="$style.top">
            <ColoredBackground :positionAbsolute="true" />

            <!-- pokud existuje top slot, tak se pak odstrani default -->
            <slot name="top" />

            <div v-if="!slots.top" :class="$style.texts">
                <h1>{{ props.text }}</h1>
            </div>
        </section>
        <section :class="$style.mainsection">
            <slot />
        </section>
    </div>
</template>

<style module lang="scss">
:root:is(.theme-light) {
    display: none;
    .sections >section:is(.top) .background-f55288d9-4dcf-456d-87c4-26be60c16cdb {
        &::after {
            opacity: 0 !important;
        }
    }
}

.sections {
    position: relative;
    min-height: calc(100vh);
    margin: 0 auto;

    >section:is(.top) {
        background-size: cover;
        width: 100vw;
        height: calc(55vh + 100px);
        top: 0;
        left: 0;
        position: relative;
        overflow: hidden;
        animation: wseiohfiusdouijasdnfodsajf 1s ease;
        animation-iteration-count: 1;



        .wallpaper {
            overflow: hidden;
            position: absolute;
            background: url("../../../public/images/bg1.png"), linear-gradient(90deg, rgba(0,112,187,1) 0%, rgba(57,90,154,1) 25%, rgba(114,68,121,1) 50%, rgba(227,24,55,1) 100%);
            background-position: center;
            background-size: cover;
            filter: blur(1vw);
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 110%;
            height: 110%;
            z-index: 0;

            &::after {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background: linear-gradient(0deg, black, transparent);
                opacity: 0.7;
                transition-duration: 0.3s;
            }
        }

        &::after {
            content: "";
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 100%;
            height: 100px;
            rotate: 180deg;
            background-image: var(--divider1);
            z-index: 1;
            background-size: 100% 100%;
        }

        >.texts {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            animation: apsldsadld 1s ease;
            animation-iteration-count: 1;



            >h1, >p {
                margin: 0;
                text-align: center;
                width: fit-content;
                margin: 0 auto;
                color: white;
            }

            >h1 {
                font-size: clamp(32px, 6vw, 96px);
            }

            >p {
                font-size: clamp(0px, 1.5vw, 24px);
                opacity: 0.7;
                margin: 8px 0;
            }
        }
    }

    >section:not(.top) {
        width: 80%;
        margin: 0 auto;
    }
}

@keyframes wseiohfiusdouijasdnfodsajf {
    0% {
        height: calc(100vh);
    }

    7% {
        height: calc(100vh);
    }

    100% {
        height: calc(55vh + 100px);
    }
}

@keyframes apsldsadld {
    from { opacity: 0 }
    to { opacity: 1 }
}

</style>
